apiVersion: v1
kind: Namespace
metadata:
  name: code-reader
  labels:
    name: code-reader
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-reader-frontend
  namespace: code-reader
  labels:
    app: code-reader-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: code-reader-frontend
  template:
    metadata:
      labels:
        app: code-reader-frontend
    spec:
      containers:
      - name: code-reader-frontend
        image: ghcr.io/your-username/code-reader-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: code-reader-frontend-service
  namespace: code-reader
  labels:
    app: code-reader-frontend
spec:
  selector:
    app: code-reader-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-reader-backend
  namespace: code-reader
  labels:
    app: code-reader-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: code-reader-backend
  template:
    metadata:
      labels:
        app: code-reader-backend
    spec:
      containers:
      - name: code-reader-backend
        image: ghcr.io/your-username/code-reader-backend:latest
        ports:
        - containerPort: 8000
        env:
        - name: APP_HOST
          value: "0.0.0.0"
        - name: APP_PORT
          value: "8000"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: code-reader-secrets
              key: database-url
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: code-reader-secrets
              key: openai-api-key
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: code-reader-secrets
              key: github-token
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        volumeMounts:
        - name: data-volume
          mountPath: /app/data
      volumes:
      - name: data-volume
        persistentVolumeClaim:
          claimName: code-reader-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: code-reader-backend-service
  namespace: code-reader
  labels:
    app: code-reader-backend
spec:
  selector:
    app: code-reader-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-reader-data-pvc
  namespace: code-reader
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: code-reader-secrets
  namespace: code-reader
type: Opaque
data:
  # 请替换为实际的 base64 编码值
  database-url: <base64-encoded-database-url>
  openai-api-key: <base64-encoded-openai-api-key>
  github-token: <base64-encoded-github-token>
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: code-reader-ingress
  namespace: code-reader
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: code-reader.your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: code-reader-frontend-service
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: code-reader-backend-service
            port:
              number: 8000
